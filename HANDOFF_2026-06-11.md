# HANDOFF 2026-06-11 (세션 인계)

> 컨텍스트 가득 차 다음 세션으로 인계. 아래 "열린 스레드"부터 보면 됨.

## ★ 지금 열린 스레드 (이어서 할 것)
**사용자 질문(2회 반복)**: 인쇄물에 "가운데 (구분)선이 안 들어간 거 보니 어제 수정이 반영 안 된 거 아닌가?"

**조사 결론 (사용자에게 답 줄 내용)**:
1. **배포는 정상.** 어제 #303(`e3d1804` "시험지-인쇄 시중 포맷풍 타이포")은 main에 있고, 오늘 #317까지 머지된 최신 운영 빌드(main HEAD)에 포함됨 → **어제 수정 반영됨.** 배포 문제 아님.
2. **가운데 세로 구분선은 별개 이슈(원래 없음).**
   - `cloud/[examId]/page.tsx:2091~2116` 시험지 문제 2단 인쇄 = **CSS Grid**(`gridAutoFlow:'column'`, `columnGap`) → **column-rule(가운데 선) 불가 → 구분선 없음.**
   - 해설 영역만 `columnCount:2 + columnRule:'1px solid #e5e5e5'` (`cloud/[examId]/page.tsx:3012`) 라 가운데 선 있음.
   - 스크린샷: 이미지1(공통수학2, 가운데선O, "과사람 With-People" 푸터 + "PAGE") = **다른 브랜드 템플릿**(위치 미확정 — `/print/**` 또는 워크시트 템플릿일 가능성, "With-People" grep 미발견). 이미지2·3(중2, "페이지 N" 푸터) = **exam-management 인쇄**(footer `page.tsx:1458 "{N}페이지"`), 가운데선 없음.
   - exam-management 2단 problem 렌더에 구분선 있는지 **확인 중 끊김** (page.tsx 1320 부근 보던 중).

**다음 행동(권장)**: 사용자가 가운데 선을 원함(이미지1처럼 통일하고 싶어함). Grid는 column-rule 불가 → **배경 linear-gradient(50%) 또는 absolute 세로선**으로 시험지 2단 문제영역에 가운데 구분선 추가. cloud + exam-management 둘 다. 단, **인쇄 기하는 fragile**(메모리 `feedback_print_measure_render_scope_sync`) — 측정/분할 로직 안 건드리고 시각 선만 추가. 먼저 사용자에게 "어제 수정은 반영됐고, 시험지 가운데 선은 원래 없었음. 추가해드릴까요?" 확인 후 진행.

## 이번 세션 머지 완료 (전부 prod 반영)
- #295 코드퀄리티1차(타입에러0·가드·vitest) / #299 **.vercelignore 핫픽스(운영빌드 복구 — `/image-pipeline/` 루트앵커, 메모리화)**
- #296 학생 옵시디언 볼트 + **rcc 읽기연동**(`.env.local` `RCC_SUPABASE_SERVICE_KEY`, 매일06:00 스케줄러, 전화번호 매칭)
- #302 시험지 자동출제 1000행 잘림(문제3천+ 후보누락) / #305 분석 객관식 분수·정수 폰트 통일
- #306 **선생님 계정 공통 시험지 미표시 — exams RLS 공통풀 허용**(마이그 `20260611_001`, prod 적용완료) + useExams limit 페이지네이션
- #307 시험지관리 상단 액션바 겹침 / #308 페이지당 컨트롤 우측 옵션패널 이동
- **#311 수업(출제관리) 페이지** `/dashboard/assignments` (매쓰플랫 "수업" 미러: 좌측 학년/반 트리 + 학습내역/학습지/보고서 탭) — API `/api/assignments`(QR print_sessions + 수동/엑셀 diagnostics.sessions 합산)
- **#312 nav IA 1단계**(8→6그룹, 흐름순 문제은행·학원자료·출제·채점·**수업**·분석. 수동채점입력 메뉴 제거, 학생화면 [수업]으로 수렴) — `PLAN_IA_CONSOLIDATION.md`
- #313 수업 보고서 탭(학생별 학습지 리포트 — 평균·점수추이·약점·출제목록)
- **#314 엑셀/수동 채점 누락 수정 — roster canonId 매핑**(diagnostics.sessions 98건 중 96건이 roster_students.id라 누락됐던 것. resolveStudent로 promoted→user 매핑)
- #316 학생성적(/tutor/analytics) 라이트→다크 테마 통일(GradeRosterTable·StudentDetailModal·Content 포함)
- #317 인쇄 보기 한글 줄바꿈(`word-break:keep-all`) + 페이지당 N 강제 잘림 완화(측정 높이로 조기분할, 잘림0 안전방향)
- 닫음: #297(rcc UI흡수 폐기 — DB diag_* DROP함) / #309(출제관리 1차 폐기 — #311로 재작업)

## 다음 단계 백로그 (사용자와 합의된 방향)
- **IA Phase 2~3** (`PLAN_IA_CONSOLIDATION.md`): [수업]을 진짜 허브 페이지로, 보고서에 진단 합산, 학부모 공유링크+인쇄(PR #275 토큰 재사용), 흡수된 단독페이지 리다이렉트.
- **매쓰플랫 로드맵** (`mathflat-research/MATHFLAT_DEEP_FINDINGS.md` + `PLAN_OUTPUT_COMMON_LINE.md`): ① HWP한글출력(LaTeX→HWP수식객체, 샘플 `mathflat-research/mathflat_sample.hwpx` 해부완료) ② 출제 공통라인(session_type 진단잠금 해제) ③ 6소스 학습지 빌더.

## ★ 주의 / 함정
- **작업트리에 이전 세션 미커밋 WIP 있음(내 것 아님, 손대지 말 것)**: `exam-analysis/[examId]/students/[studentId]/page.tsx`(M, **tsc 에러 962줄 'legacy'|undefined vs 'unified'**), `components/diagnostics/ComprehensiveReportView.tsx`(M), 미추적 `components/diagnostics/report-primitives.tsx`·`components/exam-report/`. + stash@{0} `carried-report-route-wip`. → 이 tsc 에러는 내 변경 아님. **내 파일만 골라 커밋**하면 됨(build는 ignoreBuildErrors라 통과).
- **커밋 메시지는 항상 파일(-F)로**: PowerShell here-string이 `:` `?` `"` `(` 에서 깨짐(이번 세션 여러 번).
- **브랜치 확인 후 커밋**: checkout 직후 엉뚱한 브랜치/ main 에 커밋된 사고 반복됨. `git branch --show-current` 확인.
- **프리뷰 Supabase 미연동** — DB/인증 의존 화면은 prod 머지 후 확인. 인쇄 기하도 prod 실데이터로.
- **빌드 첫 시도 SIGTERM/ENOENT `.nft.json`** = Windows 일시 오류. `.next` 지우고 재빌드하면 EXIT 0.
- MCP Supabase project_id = `ppexawiiphghdrjnmvkx`.
