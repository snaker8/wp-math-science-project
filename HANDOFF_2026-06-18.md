# HANDOFF 2026-06-18 (세션 인계)

> 긴 세션 — HML 가져오기 완성·배포 + 클라우드 편집 개선 다수 배포. 컨텍스트 정리용.
> 다음 세션은 "★ 다음 할 일"부터 보면 됨.

## 오늘 배포 완료 (전부 main 머지 = prod)
- **#349** 한글 .hml 직접 가져오기 + 룰베이스 검증루프(⚠️배지) + 고급분석 2차모달
- **#350** 문제추가 "같은 그룹" 탭(그룹 시험지 통째 합치기 + 그룹 문제) + 2차모달 이미지 도식창([도형] 인식)
- **#351** HML 표 객관식 자동 감지(choiceHeaders) — 화명중 #5 사탕문제류
- **#352** HML 검증루프에 "본문 표 포함" 플래그(좌표배치 표는 자동복원 불가 → ⚠️ 수동안내)
- **#353** HML 빈칸 네모 `box{…}` → `\boxed{…}`
- **#354** "같은 그룹" 빈목록 버그픽스 — 그룹 시험지 대부분 공통풀(institute_id NULL)인데 applyInstituteFilter가 제외+assertInstituteAccess(NULL) throw. 현재 시험지 institute로 직접 필터.
- **#355** 입력칸 autofill 흰배경/흰글자 가독성(globals.css -webkit-autofill 전역 오버라이드)
- **#356** 폴더/시험지 작업 후 재조회 silent — 전체 새로고침 깜빡임 제거(fetchData({silent}))

## 데이터 작업 (DB 직접, 배포 무관)
- **"서여고 미적분1 자료"**(exam 4c7864bf) 를 공통풀(NULL)→**엄궁차수학(본원, institute 7ef23ef9)** 으로 이동. 폴더("학교별보충자료")가 이미 본원이라 시험지+문제20개 institute만 맞춤. (엄궁차수학=과사람과 별개 학원/organization f495cbec, 산하 센터=본원 하나.)

## ★ 다음 할 일 — 클라우드 매끄러움 개선 (미착수)
사용자: "다른 수학 프로그램처럼 매끄럽게" (단 **기능 지장 없이, 별도 PR 검증**).
silent(#356)로 깜빡임은 없앴고, 다음:
1. **낙관적 업데이트** — 폴더/시험지 작업이 서버 응답까지 ~0.2초 지연 남음. temp 노드 즉시 추가→POST→성공 시 silent refetch 교체, 실패 롤백. → 0지연 체감.
2. **트리 추가/삭제 framer-motion 애니메이션**.
3. **시험지 열기 페이지 전환** 부드럽게.
- 핸들러: `CloudListClient.tsx` handleCreateGroupSave(1154)·handleConfirmRenameGroup(1192)·handleDeleteGroup(1217)·시험지 rename/move/delete.
- 메모리: `~/.claude/.../memory/project_cloud_smoothness.md`

## ★ 환경 함정 (이 세션에서 겪음)
- **bash PATH가 세션 중 깨짐** — grep/sed/head/npx/gh 가 "command not found". `git` 은 bash 동작. → tsc/build/gh/PR 은 **PowerShell** 로(`npx tsc --noEmit`, `npm run build`, `gh pr ...`). 파일경로 한글공백 때문에 PowerShell 은 cd 생략(working dir 이미 프로젝트).
- dev중 `npm run build` 금지(.next 손상). 좀비포트 3000/8100/8200 kill 후 재기동.
- 배포 = main 머지(squash)만. PR 머지 후 `gh pr merge --squash --delete-branch` → main pull.

## HML 함정 (project_hml_import_status.md 참조)
- lib(hml-*) 고치면 dev **재시작 필수**(HMR 안 물음). 재테스트는 같은 title dedup → exams.deleted_at 소프트삭제 후 재업로드.
- MCP Supabase project_id = `ppexawiiphghdrjnmvkx`.
