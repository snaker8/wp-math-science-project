# HANDOFF — 2026-05-16

> **목적**: 다음 세션이 이 파일만 읽고 즉시 작업 가능하도록 작성.
> **현재 브랜치**: `main` (PR #141 머지 직후, worktree `mystifying-jepsen-871f8d`)
> **최종 커밋**: `d3af851` Merge PR #141 / `eef8655` 빠른답 객관식 가드 보완

---

## 🚨 핵심 미해결 사고 — 진단평가(BS_H1S1_R1) 빠른답 적용 누락

### 현상
- 빠른답 OCR **미리보기** 에는 8번=`65`, 9번=`22`, 37번=`30` 등 단답형 정답이 **정상 추출**.
- **「적용」 버튼 클릭 후** 정답표를 보면 **일부 문제 답이 `-` 로 누락**.
- 인식은 전부 됐는데 DB 박힘 단계에서만 빠짐.

### 적용된 수정 (PR #141)
- `src/app/api/exams/[examId]/match-answers/route.ts:300`
- 가드 보완: `problemIsObjective && looksObjective` 둘 다 충족할 때만 `normalizeObjectiveAnswer` 적용.
- `looksObjective = /^[①②③④⑤]$/.test(ans) || /^[1-5]$/.test(ans)`
- 즉 OCR=`65` (2자리 단답) → 객관식 normalize 안 탐 → 그대로 박힘 (의도).

### 그런데 사용자 보고: 머지 후에도 일부 답 안 나옴
→ **PR #141 만으로 부족**. 다른 경로에서 답이 강등되고 있을 가능성.

---

## ▶ 다음 세션에서 먼저 할 일 (순서 엄수)

### Step 1 — DB 직접 확인 (화면 캐시 vs 실제 박힘 분기)

```sql
-- BS_H1S1_R1 시험지 ID 먼저 확보
SELECT id, title, created_at
  FROM exams
 WHERE title ILIKE '%BS_H1S1_R1%'
 ORDER BY created_at DESC
 LIMIT 5;

-- 해당 시험지 problems 의 answer_json 직접 조회
SELECT ep.sequence_number,
       p.id,
       p.answer_json->>'finalAnswer'      AS final_answer,
       p.answer_json->>'correct_answer'   AS correct_answer,
       p.answer_json->>'type'             AS type,
       p.answer_json->'choices'           AS choices,
       p.answer_json->>'answer_user_edited' AS user_edited,
       p.answer_json->>'uploaded_at'      AS uploaded_at
  FROM exam_problems ep
  JOIN problems p ON p.id = ep.problem_id
 WHERE ep.exam_id = '<EXAM_ID>'
 ORDER BY ep.sequence_number;
```

**판정**:
- `finalAnswer` 가 `''` 또는 `null` → **DB 강등 발생** → Step 4 로 (가설 A/B/C)
- `finalAnswer` 에 값은 박혀 있는데 화면 `-` → **프론트 표시 캐시 / 다른 컬럼 읽음** → Step 4 가설 D 로

### Step 2 — PR #141 fix 가 main 에 실제 들어갔는지 검증

```bash
git log --oneline main -5
# d3af851 Merge pull request #141 ... 확인
# eef8655 fix(match-answers) ... 확인

grep -n "looksObjective" src/app/api/exams/\[examId\]/match-answers/route.ts
# line 297 부근에 가드 존재해야 함
```

### Step 3 — Vercel 빌드 결과 확인

- `vercel deployments list` 또는 대시보드.
- PR #141 머지 후 빌드 성공·운영 적용됐는지.
- 사용자가 본 사고가 **이전 빌드** 에서 나온 거면 PR #141 검증 무의미.
- **반드시 production URL 의 최신 deploy 시각을 사용자 사고 발생 시각과 비교**.

### Step 4 — 가설 순서로 디버깅 (A → D)

아래 「사고 가설」 섹션 따라 1개씩 검증. **A 부터**. 한 번에 여러 개 고치지 말 것.

---

## 🔍 사고 가설 (우선순위 순)

### 가설 A — 미리보기 → PUT 사이 `hasChange` 필터로 `newAnswer` 누락 ★ 최우선

**위치**: `src/app/api/exams/[examId]/match-answers/route.ts:207`

```ts
const hasChange = Boolean(
  (newAnswer && newAnswer !== currentAnswer) || (newSolution && newSolution !== currentSolution)
);
```

**의심점**:
- POST 미리보기 시점 `currentAnswer` 가 이미 `'65'` 처럼 박혀 있고 OCR 도 `'65'` → `hasChange=false`.
- 프론트가 `hasChange=true` 인 행만 PUT 으로 보내면 → 「적용 후에도 값 그대로」 처럼 보일 수 있음.
- 단, 사용자 증상은 "`-` 로 표시" 이므로 시나리오가 다름. **하지만 일부 문제만 누락되는 패턴에 가까우니 1순위 점검**.

**검증**:
1. 프론트 (`/dashboard/exam-management/.../page.tsx` 같은 곳) 에서 PUT body 만들 때 `m.hasChange` 필터링 여부 grep.
2. `Grep` 패턴: `hasChange.*filter|filter.*hasChange|matches.filter`
3. 필터 발견 시 — 본 케이스(현재값 빈, 새값 있음) 누락 가능성 점검.

### 가설 B — `answer_user_edited` 보호 플래그 충돌

**위치**: 동일 파일 line 305 `mergedAj.answer_user_edited = true`

**의심점**:
- PUT 핸들러는 항상 `mergedAj.answer_user_edited = true` 박음.
- 만약 **이미 user_edited=true 인 문제** 를 빠른답으로 덮어쓰려 할 때 다른 곳에서 막혀 있는지.
- 일괄 재생성 / classify / generate-solution 류에서 `answer_user_edited` 체크하고 스킵하는 로직이 빠른답 적용 자체를 방해하지는 않는지.

**검증**:
1. `Grep -rn "answer_user_edited" src/`
2. PUT 직후 다른 잡(예: 자동분류 트리거)이 `mergedAj` 를 덮어쓰지는 않는지.

### 가설 C — PUT 핸들러의 미검토 가드

**위치**: `src/app/api/exams/[examId]/match-answers/route.ts:272`

```ts
for (const match of matches) {
  if (!match.newAnswer && !match.newSolution) continue;
  ...
}
```

**의심점**:
- 프론트에서 `newAnswer: ''` 빈 문자열로 보냈을 때 truthy 체크라 skip 됨 — 의도된 동작.
- 하지만 PR #141 가드 진입 전 `normalizeObjectiveAnswer` 동작 흔적이 남아 있는 다른 경로 존재 가능.
- `src/lib/validation/objective-answer.ts` 의 `normalizeObjectiveAnswer` 가 빠른답 외 어디서 호출되는지.

**검증**:
1. `Grep -rn "normalizeObjectiveAnswer" src/`
2. 빠른답 PUT 외 다른 API (예: `/api/admin/reocr-points`, `/api/problems/.../generate-solution`) 에서 빠른답 적용 직후 추가로 호출돼 강등시키는지.

### 가설 D — 진단평가 특화 분기 (`is_diagnostic`)

**의심점**:
- BS_H1S1_R1 은 진단평가 — `exams` 또는 `problems` 에 `is_diagnostic` 같은 컬럼/플래그 존재 시 빠른답 핸들러가 다른 분기 탈 수 있음.
- 또는 진단평가 전용 정답표 렌더링 컴포넌트가 `answer_json.finalAnswer` 가 아닌 다른 필드 읽고 있을 수 있음.

**검증**:
1. `Grep -rn "is_diagnostic\|diagnostic" src/app/api/exams src/lib`
2. 진단평가 정답표 화면 컴포넌트 path 확인 후 어떤 필드 읽는지.
3. 화면 `-` 표시 로직 grep: `Grep -rn "['\"]-['\"]" src/components` (정답 fallback).

---

## 📦 오늘 머지된 14개 PR 정리 (#128~#141)

| PR | 요약 |
|---|---|
| #128 | 가맹 학원 신청 플로우 (Phase 1) |
| #129 | 리브랜딩 — 과사람 → Math×Sci Bank |
| #130 | super_admin 뱃지 |
| #131 | 학생 직접 등록 시 institute_id 전달 (Database error 차단) |
| #132 | users_pkey 중복 차단 (INSERT → upsert ignoreDuplicates), v_role cast |
| #133 | 학생 ID = 전화번호, 초기 비번 123456, 학원 전용 로그인 URL |
| #134 | 학생 자동 이메일 'already registered' 차단, 반 미배정 학생 목록 노출, UI 이메일 형식 숨김 |
| #135 | 학생 CRUD (수정·비번 초기화·삭제·soft delete), createUser user_metadata 동봉 |
| #136 | 가맹 학원 신청 승인/거부 화면 (Phase 2) |
| #137 | 비밀번호 변경 UI (공통) |
| #138 | 잔여 '과사람' → Math×Sci Bank |
| #139 | students 라우트 슬러그 충돌 — `[id]` → `[studentId]` |
| #140 | 채점표 이미지 OCR 자동 채점 (PR-B) |
| #141 | 빠른답 OCR 단답이 객관식 가드에 빈값 강등되는 사고 차단 ← **현재 미해결 사고 관련** |

### 운영 데이터 정리 (오늘)
- 학생 ID 체계: 전화번호 기반 자동 이메일 (`<phone>@<institute>.mathscibank.local` 류) + 초기 비번 `123456`.
- 학원 전용 로그인 URL: `/login?org=<id>`.
- 가맹 학원 신청/승인 플로우 Phase 1+2 모두 운영 적용.
- 학생 목록: institute 기반 fetch 로 전환 (반 미배정 학생도 노출).

---

## ⚠ 다음 세션 주의사항

### 1. PR 머지 후 동일 브랜치 push 절대 금지 (오늘 4번 재현)

오늘 다음 패턴 4번 발생:
- #131 머지 후 동일 브랜치 추가 push → 고아 커밋 → 새 PR #132 별도 오픈해서 복구
- #133 머지 후 → #134
- #134 머지 후 → #135
- #138 머지 후 → #139 (build 깨짐, slug 충돌)

**규칙**:
- 머지된 브랜치는 즉시 버리고 main 에서 새 브랜치 파기.
- 메모리: `feedback_post_merge_push_warning.md` (이미 박혀 있음).

### 2. 객관식 박힘 차단 가드 정책 유지

- `feedback_objective_answer_safety.md` 가 원칙. ①~⑤/1~5 외 값은 객관식 답으로 절대 박지 말 것.
- PR #141 은 이 원칙을 **무력화** 하지 않음 — **객관식 형식 OCR 일 때만** normalize 작동.
- 다음 세션에서 사고 추가 수정 시 이 가드를 풀지 말 것. **OCR 결과 형식 판단으로 우회**.

### 3. 운영 핵심 가드 그대로 두기
- `CLAUDE.md` 의 "안전 가드" 섹션 (8개) 은 전부 사고 학습 결과 — 건드리려면 영향 범위·잃는 것 명시 후 명시적 승인.
- 메모리: `feedback_no_structural_change_without_explicit_approval.md`.

### 4. Supabase select 1000 limit 주의
- `.in()` + 1000+ rows 잘림. BS_H1S1_R1 문제 수가 1000 미만이면 무관하지만, 진단평가 전체 집계 시 페이지네이션 필요.
- 메모리: `feedback_supabase_select_limit.md`.

---

## 📍 핵심 파일 위치

| 영역 | 경로 |
|---|---|
| 빠른답 API (POST 미리보기 / PUT 적용) | `src/app/api/exams/[examId]/match-answers/route.ts` |
| 객관식 normalize | `src/lib/validation/objective-answer.ts` |
| 빠른답 프론트 (적용 버튼) | 정확 위치 grep 필요: `Grep -rn "match-answers" src/app src/components` |
| 진단평가 정답표 화면 | `src/app/dashboard/prescription/` 또는 `src/app/dashboard/exam-management/` 하위 |
| 진단평가 스키마 | Supabase `diagnostics.*` (CLAUDE.md 참조) |

---

## 🔧 디버깅 코드 스니펫 (즉시 사용)

### PUT 핸들러 진입 로그 추가 (가설 A·C 검증)

```ts
// src/app/api/exams/[examId]/match-answers/route.ts:271 직후
console.log('[match-answers PUT] received matches:',
  matches.map(m => ({
    pid: m.problemId,
    newAns: m.newAnswer,
    newSol: m.newSolution ? `${m.newSolution.length}자` : null,
  }))
);
```

### finalAnswer 박힘 확인 (가설 B 검증)

```ts
// PUT update 직전
console.log('[match-answers PUT] update:', match.problemId, {
  finalAnswer: mergedAj.finalAnswer,
  type: mergedAj.type,
  hasChoices: Array.isArray(mergedAj.choices),
  user_edited: mergedAj.answer_user_edited,
});
```

### Vercel 로그 확인

```bash
vercel logs --follow
# 또는 deployment 별
vercel logs <deployment-url>
```

---

## 🎯 세션 시작 시 첫 메시지 (다음 클로드용)

> 진단평가 BS_H1S1_R1 빠른답 적용 누락 사고 디버깅 이어서 진행합니다.
> PR #141 머지 후에도 일부 답이 `-` 로 빠지는 현상 잔존.
> Step 1 (DB 직접 SQL) → Step 2 (git verify) → Step 3 (Vercel build) → Step 4 가설 A 검증 순서로 진행하겠습니다.
> 가설 A 부터 시작합니다 — 프론트 PUT body 의 `hasChange` 필터 위치 grep 부터.
