# 핸드오프 — 2026-06-01 (다음 세션 인계)

## ⚠️ 작업 원칙 (반드시 지킬 것)
- **운영 DB·자산화 핵심(OCR·분류·cloud-flow)·미들웨어는 승인 없이 절대 건드리지 말 것**
- 순서: **진단 → 보고 → 승인 → 적용**. 읽기·분석은 자유, 쓰기는 승인 후.
- **도구 호출 시 정확한 형식 준수** — 지난 세션에서 `call`/`invoke` 같은 깨진 형식이 화면에 새어 멈춤 사고 반복. antml 형식 정확히.
- 무거운 명령(`npx tsc` 5분) 자제 — 사용자가 멈춘 줄 알고 끊음. 짧은 명령 위주.
- **Supabase project_id: `ppexawiiphghdrjnmvkx`** (수학홈페이지)

---

## 🔴 지금 급한 작업: 인쇄 시 문제/해설 페이지 넘침·잘림 (미착수)

### 증상 (사용자 캡처)
시험지 "인쇄/출력" 하면:
- **첫 페이지가 거의 비어있고** 문제가 안 채워짐
- 내용이 아래(해설지 영역)로 밀려 1·6·7번이 몰려 나옴
- 문제/해설이 페이지 경계에서 **넘어가거나 잘림**

### 진단 방향 (다음 세션 여기서 시작)
- **위치**: `src/app/dashboard/cloud/[examId]/page.tsx` — 페이지 분할(pagination) 로직
- **핵심 의심 커밋**: `80deafa ui(시험지·해설지): questi 양식 적용 — column-balance·자동 행간`
- **봐야 할 변수/함수** (grep 키워드): `measureWidth`, `problemHeights`, `CONTENT_H`, `FIRST_CONTENT_H`, `perPage`, `measured`, `getEffectiveGap`, `getWritingSpace`, `pages.push`, `A4_H`, `A4_W`, `PAGE_PAD`, `COLUMN_GAP`
- **가설**: 문제 높이 측정(`problemHeights`/`measured`)이 실제 렌더 높이와 안 맞아 분할 계산이 틀어짐. 특히 cases padding(아래 #259 변경)으로 문제 높이가 커졌는데 측정값이 옛날 값이면 첫 페이지를 비우고 밀어버림 → **#259 cases padding 3em 추가가 측정과 불일치 유발 가능성**.
- **실측 권장**: Claude in Chrome 으로 운영 사이트(로그인된 탭) DOM 측정. dev/preview 는 미들웨어가 로그인 리다이렉트 → 로그인 필요(비번 입력 금지, 사용자 탭 활용).
  - 사용자 운영 URL 예: `https://wp-math-science-project.vercel.app/math/dashboard/cloud/<examId>`
  - 측정법: `.exam-page` 각 문제 div 의 실제 높이 vs 측정 로직이 쓰는 높이 비교.

### 주의 (CLAUDE.md 가드)
- 이 페이지네이션은 여러 번 수정된 영역. 측정·분할 로직 변경 시 회귀 위험 큼.
- cases padding(#259) 과 상호작용 의심 — 측정 시점에 padding 반영됐는지 확인.

---

## ✅ 이번 세션 완료된 것

### 1. 주례 기말 대수 분리 → 병합 완료 (운영 DB)
- 원인: 67문항 파일 자산화가 **52개에서 Vercel 504 timeout** 으로 끊김 → 재시도 → 새 exam 생성 → 분리.
- 두 exam: `9c405391...`(대수-1, seq 1~52) + `88654e4b...`(대수-2, seq 1~12=원본 53~63번)
- **조치**: 대수-2 의 12문제를 대수-1 로 이동(seq 53~64) + 대수-1 제목 "주례 26년도 기말 (대수)" + 대수-2 soft-delete. **1~64번 한 시험으로 병합 검증 완료**.
- 복구 스냅샷: 대수-2 의 12개 exam_problem_id 는 세션 로그에 기록됨 (문제 시 원복 가능).

### 2. 자산화 PUT timeout 가드 — PR #261 머지됨
- `analyze/[jobId]/page.tsx` 자산화 PUT 에 `AbortController(295s)` + timeout 시 "재시도 말고 클라우드 확인" alert.
- 504 떠도 분리·중복 사고 차단. 서버·DB 변경 없음.

### 3. 시험지 cases/분수 잘림 — PR #259 머지됨 (이미 main 반영)
- `globals.css` + cloud page 3곳: `.exam-page .katex-display:has(.mtable) { padding-top: 3em; padding-bottom: 0.5em }`
- 운영 DOM 측정으로 검증: cases 솟음 42px→1px. 카드뷰·일반수식 영향 0 (`.exam-page` 한정, `:has(.mtable)` 한정).
- **★ 이 padding 이 위의 "인쇄 페이지 넘침" 과 상호작용할 수 있음 — 측정 로직이 이 높이 증가를 반영하는지 확인 필요.**
- 객관식 이미지 보기 표시도 같은 PR 에 포함(ExamProblemRenderer choiceImages 렌더 + exam-management 매핑).

### 4. 504 근본 원인 = Vercel Hobby 60초 벽 → **사용자 Pro 업그레이드 완료**
- 코드엔 `maxDuration = 300` 있으나 Hobby 는 60초 상한이라 무시됨 → 504.
- 사용자가 **Pro 업그레이드 완료**. 재배포(PR #259/#261 머지로 자동 배포) 후 300초 적용.
- `string did not match the expected pattern` 에러도 같은 504 뿌리 → Pro 로 해결 예상.
- **미확인**: Pro 적용 후 실제 67문항 파일 재자산화 통과하는지 테스트 (사용자 몫).

---

## 🟡 보류 — 승인 필요 (제가 임의로 못 함)

### 로그인 `/dashboard` role 가드 누락 (보안)
- **현상**: 로그인 탭(학생/학부모/선생님)은 인증에 무관 — 안전. 단 `src/middleware.ts` 의 `ROUTE_PERMISSIONS` 에 **`/dashboard` 가 없음** → STUDENT/PARENT 계정도 URL 로 `/dashboard`(선생님 화면) 진입 가능.
- **데이터 유출 여부**: 화면은 열려도 API 는 `institute-guard`/RLS 가 막음. 단 메모리에 "25+ 파일 institute-guard 누락" 이력 → 일부 샐 수 있음.
- **수정안**: `ROUTE_PERMISSIONS` 에 `/dashboard: ['ADMIN','ORG_ADMIN','TEACHER','TUTOR']` 추가.
- **⚠️ 미들웨어는 모든 페이지 접근에 영향 — 잘못 막으면 선생님도 못 들어감. 승인 + 신중 검증 후 PR.**

---

## 현재 git 상태
- 작업 브랜치: `claude/school-archive` (10개 미커밋 — 어제 학교자산화 PR #231 draft, 건드리지 말 것)
- main 최신: `3a56d15`(#261 timeout) ← `bd99882`(#259 cases/이미지)
- 열린 worktree 잔여: `.claude/worktrees/` 아래 잠금 디렉토리들 — gitignore, 무해. git worktree prune 됨.

## 참조
- 이전 핸드오프: `HANDOFF_2026-05-30.md`
- 메모리: `~/.claude/projects/.../memory/MEMORY.md` (가드 #5 Vercel chain, cases display size 등)
