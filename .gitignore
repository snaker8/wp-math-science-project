# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Next.js
.next/
out/
build/
dist/

# Production
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# ★ 2026-08-30: 사본·백업이 무방비였다. `.env.local.sydney-backup`(서울 이전 전 시드니 키
#   전량: SUPABASE_SERVICE_ROLE_KEY·ANTHROPIC·OPENAI·MATHPIX 등)이 위 규칙에 안 걸려
#   `git add .` 한 번이면 커밋될 상태로 방치돼 있었다. 접미사가 뭐든 막는다.
#   (`.env.example` 은 이 패턴에 안 걸리므로 계속 추적된다.)
.env.local.*
.env.*.backup
.env.*.bak

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
nul

# Misc
*.tsbuildinfo
.claude/

# Graphify knowledge graph output
graphify-out/

# Python 캐시
__pycache__/
*.pyc
*.pyo

# 로컬 도식 이미지 (3.1GB) — Supabase Storage로 이관됨
image-pipeline/dasaram_diagram_db/images/
image-pipeline/dasaram_diagram_db/metadata/
image-pipeline/dasaram_diagram_db/upload_checkpoint.json

# 교재 해설 데이터 (크기/라이선스)
curriculum_data/textbook_solutions/

# 업로드/추출 임시 파일
image-pipeline/dasaram_diagram_db/*.tmp
image-pipeline/*.log
.vercel

# 진단 시스템 웹챗 원본 (참조용, 커밋 X)
개별맞춤학생프로젝트/
insane-design/*/css/
insane-design/*/index.html
insane-design/*/screenshots/
insane-design/*/phase1/

# 일괄 적재 드라이런 리포트 (실행 시 생성 — 커밋 대상 아님)
scripts/import-report-*.txt
scripts/_tmp-*

# 일괄 교정 스크립트가 뜨는 원본 백업 — 문제 본문이 들어 있고 크다. 복구용 로컬 파일.
scripts/.backup-*.json
