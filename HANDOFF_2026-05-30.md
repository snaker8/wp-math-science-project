# 핸드오프 — 2026-05-30 (다음 세션 인계)

## ⚠️ 작업 원칙 (사용자 강하게 강조 — 반드시 지킬 것)
- **운영 DB·수학 워크플로우 핵심(OCR·분류·cloud-flow)은 승인 없이 절대 건드리지 말 것**
- 순서: **진단 → 보고 → 승인 → 적용** (어제 운영 DB를 추측으로 건드려 사고 남)
- **잘 되던 것 망치지 말 것**. 읽기·분석은 자유, 쓰기는 승인 후.

---

## 운영 DB 상태: 정상 ✅
- 프로젝트: **수학홈페이지** (`ppexawiiphghdrjnmvkx`)
- 진단평가A(`b8b15752`) / 진단평가B(`e8c83722`): 각 25문제 — 연결·분류 정상
- **search_path: "따옴표 제거" 상태 = 정상** (자산화 트리거 `calculate_exam_total_points` 정상)
  - 어제 이력: 고침(따옴표 제거) → 사용자요청 되돌림(따옴표) → **0문항 사고 발견** → 재수정(따옴표 제거, 현재)
  - 🚨 **search_path 따옴표 버전 = 자산화 깨짐(exam_problems 0). 절대 따옴표로 되돌리지 말 것.**
  - 검증식: `SELECT cfg FROM pg_proc ... WHERE proname='calculate_exam_total_points'` → `search_path=public, pg_catalog` (따옴표 없어야 정상)

---

## ✅ 완료: #22 — "1차 OCR 수정이 자산화 시 또 깨짐" (저번주 회귀 주범)

### 문제
1차 OCR 화면(analyze 페이지)에서 손으로 고쳐도, 자산화 시 `verifyAndRepairWithVision`(크롭 이미지 기준 재교정)이 사용자 수정을 도로 덮어씀 → "고쳐도 또 깨짐". 5/27 커밋 `764abd3`(비전 검증 추가)부터 발생. 사용자 "저번주부터"와 시기 일치.

### ★ 추가 진단 (2026-05-30 세션) — 비전 교정 호출이 **2곳**
- `saveEditedProblemsDirect` (L1791, autoCrop 경로 — `jobResults` 비어있을 때)
- `saveProblemsToDB` (L2621, OCR-results 경로 — `jobResults` 채워졌을 때)
- 표준 OCR 업로드(jobResults 채움) → analyze 수정 → 재자산화 시 **L898 saveProblemsToDB 경로**를 탐.
  → 핸드오프 원처방(saveEditedProblemsDirect만)은 **절반만 막힘**. CLAUDE.md 가드 #4/#6/#7 "두 함수 동시 패치 필수" 교훈 그대로.

### 처방 (✅ 완료 — 별도 PR #245, main 기준)
- PR: https://github.com/snaker8/wp-math-science-project/pull/245 — branch `claude/fix-22-vision-overwrite`
- 변경 (두 함수 모두 패치):
  1. analyze page: `editedProblems` 에 `isEdited: p.status === 'edited'` 전달
  2. `saveEditedProblemsDirect`: `isEdited` 면 비전 재교정 스킵
  3. PUT 오버라이드 루프(L726): 매칭된 `result` 에 `__userEdited` 플래그 마킹(번호매칭 실패 무관)
  4. `saveProblemsToDB`: `result.__userEdited` 면 비전 재교정 스킵
- 미수정(자동) 문제는 기존대로 비전 교정 유지. 변경 2파일 타입체크 0 에러.

### 남은 단계
1. **검증 (배포 전 필수)**: PR #245 Vercel 프리뷰 또는 로컬에서 — 1차 OCR 화면 한 문제 수정 → 자산화 → DB `content`가 수정본 그대로인지 확인. 서버 로그 `사용자 수정본 → 비전 재교정 스킵` 출력되면 OK.
2. **머지**: 검증 후 PR #245 → main 머지하면 prod 반영.

### ⚠ school-archive 워킹트리 정리 필요 (#245 머지 후)
- 현재 `claude/school-archive` 워킹트리(미커밋)에는 **원처방의 부분 #22**(saveEditedProblemsDirect 가드만, route.ts L1777~1801)가 학교자산화 작업과 섞여 남아있음.
- #245(완전판)가 main 머지된 뒤 school-archive 를 rebase 하면 이 부분 #22 가 중복/충돌 → **그때 school-archive 의 부분 #22 훅을 드롭**할 것. (완전판은 #245 가 authoritative)

---

## 미커밋 변경 (브랜치: `claude/school-archive`)
- 어제 학교 자산화 작업 (route.ts, folder-import, CloudFlowUploader, auto-folder, folder-meta-extract, workflow.ts) — **PR #231 (draft)**
- 방금 #22 변경 (route.ts, analyze page.tsx) — 위 처방
- `supabase/migrations/20260529_001_fix_function_search_path_quoting.sql` — 운영 DB와 동기화 주의(되돌림/재수정 이력 있음). 현재 운영 = 따옴표 제거.

---

## 남은 작업 (우선순위)
1. **#22** — ✅ **main 머지 완료 (PR #245, 2026-05-30).** 운영 배포됨. 1차 OCR 수정본 비전 덮어쓰기 차단(두 함수 패치).
2. **#23** cases 분수 행 겹침 — ✅ **main 머지 완료 (PR #246, 2026-05-30).** 운영 배포됨.
   - 진단: "CSS만으로 행간" 전제는 **불가능**(KaTeX 절대위치 vlist, dev 렌더로 padding/line-height/축소 실패 실증).
   - 해결(★ **cases-only, 행렬 불변**): `MathRenderer.dfracInCases()` 가 **cases 환경 안에서만** `\frac`→`\dfrac` + `globals.css` 는 `.math-content .katex .mtable .col-align-l .mfrac{1em}` 로 **cases(왼쪽정렬)만** 확대 무력화.
   - ★ 행렬(pmatrix=`col-align-c`)은 변환·무력화 **둘 다 안 걸림** → frac+1.4em 그대로. computed-style 로 행렬 .mfrac=1.4em / cases .mfrac=1em 수치 확인. ("행렬 건드리지 말 것" 사용자 지시 반영.)
   - 전역 1.4em 은 dfrac 안 거치는 경로(풀이박스·인쇄·모달) 위해 유지 → 블래스트 반경 0.
3. **㉠ 교정 회귀** — `(ㄱ)→㉠` 단순치환 복원이 2번 Revert됨(`753171c`,`6e14d1f`, "진짜 (ㄱ) 시험지 깰 위험"). 대체된 `symbol-detector`(`32abef9`)는 Gemini Flash가 크롭에서 ㉠ 봐야만 교정 → 이 PDF에서 Flash가 못 잡음. 진단: Flash 판별 실패 원인(크롭품질/프롬프트/임계). **수학 라인, 승인 후.**
4. **#19** storage 한글 파일명 key invalid (페이지 이미지 업로드 실패, 텍스트 자산화엔 무관)
5. **#20** grade 자동 채움 견고화 (폴더 학년토큰 없으면 고1 기본값)
6. **#21** 자동(autoClassify) 경로 크롭 이미지 생성 (현재 bbox만 저장, 크롭 미생성)

---

## 학교 자산화 기능 (PR #231, draft) — OCR 정확해지면 의미 살아남
- PR-1: exams 6컬럼 + problems.source_label + 자동추출
- PR-2: 폴더 일괄 자산화 페이지 (`/dashboard/cloud/folder-import`) + 중복검사 API
- PR-3: exam-create 학교기출 4-필터 + RegionPicker
- 추가: 학교명 폴더 자동생성(findOrCreateSchoolFolder), CloudFlowUploader 학교메타 입력, 분할 제거(통째 OCR), 일련번호 통일
- ⚠ 전제: OCR/문제감지가 정확해야 이 위에서 의미 있음. 동래중 단원집은 "N PAGE" 헤더 섞임으로 OCR 깨짐(다른 창에서 감지 작업 중).
