# 매쓰홀릭 — 반 허브 ▸ 유형분석 (숙달 매트릭스) (2026-09-02 실측)

URL: `teacher.matholic.com/course/{courseId}/ug-score`
계정: 엄궁차수학(umgungsmt) · 프리미엄 · 반 = "확률과 통계 -개념진도반"

> ★★ **06 문서의 "다음 조사" 1순위.** 우리가 만들려는 "약점 찾기 → 메꾸기" 의 **입구 화면**.
> 우리에겐 이 화면이 없다. (우리 `단원별 유형 숙달` 은 트리 + α/β/γ 막대까지고, 난이도 축이 없다)

---

## 1. 골격 — 행 = 소단원 × 열 = 난이도

| | 개념 | 기본 | 실력 | 심화 |
|---|---|---|---|---|
| **1.1.1 중복순열** | ▣▣▣ | ▣▣▣ | ▣▣▣▣▣▣▣▣▣▣ | ▣▣▣▣▣▣ |
| **1.1.2 같은 것이 있는 순열** | ▣▣▣ | ▣▣▣▣ | ▣ × 17 | ▣ × 12 |
| **1.2.1 중복조합** | ▣▣▣▣▣ | ▣▣▣ | ▣ × 21 | ▣ × 12 |

- **칸 하나 = 유형 하나.** 소단원 안에서 난이도별로 몇 개인지가 한눈에 보인다.
- 좌측 각 행에 `유형그룹 선택 +` — 행 단위 일괄 선택.
- 행·열 헤더에 체크박스 → **덩어리 선택** 가능.

### 난이도 축
- 기본 **4단계**: `개념 · 기본 · 실력 · 심화`
- 상단에 `4단계 / 6단계` 토글 — 6단계면 `실력 하/중`, `심화 하/중` 으로 쪼개진다.
  (상단 난이도 칩: `개념 · 기본 · 실력 하 · 실력 중 · 심화 하 · 심화 중`)

---

## 2. 색 = 그 학생의 숙달 상태 (범례 실측)

우측에서 학생을 고르면 전체 칸이 그 학생 기준으로 칠해진다.

| 표시 | 뜻 | 김태한 실측 |
|---|---|---|
| ⭐ 초록+별 | 마스터 | **59** |
| 🟩 초록 | 잘함 | **176** |
| 🟨 노랑 | 불안정 | **119** |
| 🟥 빨강 | 약점 | **3** |
| 💀 해골 | 심각 | 0 |
| ⬜ 회색 | 미학습 | **100** |
| ？ | 판정 불가 | 0 |

**합계 457 유형** — 한 과목(확률과 통계) 한 반 기준.

### ★ 모양이 두 종류다 — 사각형 vs 원형
화면 안내: **"● 모양은 AI 예측 유형 분석결과입니다."**

- **사각형** = 실제로 풀어서 나온 결과
- **원형** = **안 풀었지만 AI 가 예측한 숙달도**

→ 이게 결정적이다. 진단을 조금만 해도 **매트릭스 전체가 채워진다.**
   안 그러면 457칸 중 대부분이 회색이라 화면이 쓸모없어진다.
   상단 `분석 방법: AI 예측` 토글로 켜고 끈다.

---

## 3. ★ 칸 클릭 → 과제로 (루프의 연결점)

칸을 누르면:
1. **툴팁**에 `유형 13 · 같은 것이 있는 순열` + **대표 문제 본문**이 뜬다
   (실측: "average의 7개의 문자를 일렬로 나열할 때, 2개의 a 사이에 홀수 개의 문자가 놓이도록 나열하는 방법의 수를 구하시오.")
2. 우측 패널 `선택된 유형` 카운터가 **0 → 1**
3. **`과제 만들기` 버튼이 비활성 → 활성**

→ **약점 칸을 눌러 모은 뒤 그대로 과제로 내보낸다.** 화면을 떠나지 않는다.

우측 패널 구성:
```
선택된 유형
    1
[ 과제 만들기 ]     ← 0개면 비활성
이름전체보기  (토글)
[ 김태한 ]          ← 학생 목록. 고르면 그 학생 색으로 다시 칠함
[ 염예진 ]
```

---

## 4. 상단 필터

| 줄 | 항목 |
|---|---|
| 교재 | `교재서 · 교재유형 · YBM · 교과공통 100 · 교과공통 300 · 동아 · 미래엔 · 비상 · 지학사 · 천재(전)` |
| 난이도 | 칩 6종 + `난이도구분 4단계/6단계` |
| 분석 | `분석 방법: AI 예측` |
| 모드 | `과제 / 수업` |
| 우측 | `초기화` · `수업모드` · (우상단) `AI 유형분석` |

- **교재별로 매트릭스가 바뀐다** — 같은 단원이라도 교재마다 유형 구성이 다르다.
- `과정 전체 보기` 토글 — 진도 나간 데까지만 볼지, 과목 전체를 볼지.
- `유형분석 시작일: 2026-03-14` — 이 날짜 이후 학습만 집계.

---

## 5. 우리 대비 — 무엇이 없나

| 매쓰홀릭 | 우리 (`/dashboard/prescription`) |
|---|---|
| **행=소단원 × 열=난이도 매트릭스** | **없음.** 트리 드릴다운(대→중→소→유형) + α/β/γ 막대까지 |
| 칸=유형 하나, 색=숙달 | 유형 리프에 색은 있으나 **난이도 축이 없다** |
| **AI 예측으로 빈칸 채움** | 없음 (시험 본 유형만 표시 → 대부분 비어 보임) |
| **칸 선택 → 과제 만들기** | 2026-09-02 `출제` 링크 추가(소단원 단위). **유형 단위 다중 선택은 없음** |
| 교재별 매트릭스 전환 | 없음 |
| 대표 문제 미리보기 툴팁 | 없음 |

### 우리 데이터로 되는가
- **행(소단원)**: `mathsecr_types` level3 — 있다.
- **열(난이도)**: `classifications.difficulty` 1~10 — 있다. 4단계로 접으면 된다.
- **칸(유형)**: level4 세부유형 — 있다.
- **색(숙달)**: `diagnostics.student_node_status` (α/β/γ) — 있다. 63명·3,950건.
- **대표 문제**: 유형 코드로 `problems` 조회 — 있다.
- ✕ **AI 예측 채움**: 없다. 안 하면 회색이 대부분일 것 — **다만 이건 비용이 드는 기능이라 별건.**

→ **AI 예측을 빼면 나머지는 지금 데이터로 만들 수 있다.**

---

## 5-1. `과제 만들기` 모달 — 루프의 끝 (실측)

★ **취소만 하고 실제로 내보내지 않았다** (누르면 학생에게 과제가 나간다).

```
과제 옵션 선택

  과제명       [(2026.09.02) 1.1.2 같은 것이 있는 순열]          ← 자동 생성

  추천 과제명   (과제시작일) 출제단원  (2026.09.02) 1.1.2 …   [적용]
               출제범위             1.1.2 같은 것이 있는 순열 [적용]
               (과제시작일) 수업명   (2026.09.02) 확률과 통계… [적용]

  시작일~마감일 [2026-09-02] [17:16]  ~  [2026-09-27] [17:16]
               마지막 설정 기간  2026-08-21 00:00 ~ 2026-09-14 23:59  [적용]

  출제방식      ● 동일유형(문제 선택)   ○ 추천유형   ○ 선택유형
               "학생 모두에게 같은 유형의 같은 문제를 과제로 출제합니다.
                선택한 유형 내에서 문제를 골라서 과제로 출제합니다."

  유형 당 출제 문제수   ● 1문제   ○ 2문제

                                                    [ 다음 ]
```

### 눈여겨볼 것
- **과제명을 세 가지 방식으로 자동 제안**하고 각각 `적용` 버튼. 타이핑이 없다.
- **`마지막 설정 기간` 재사용 버튼** — 매번 날짜 찍는 수고를 없앤다. (04 문서의 학습지 설정에도 같은 패턴)
- **출제방식 3종**
  | 방식 | 뜻 |
  |---|---|
  | 동일유형(문제 선택) | 전원에게 **같은 문제**. 선택 유형 안에서 교사가 고른다 |
  | 추천유형 | 시스템이 고른다 (미확인) |
  | 선택유형 | 유형만 지정, 문제는 학생별로? (미확인) |
- **유형 당 1~2문제** — 유형 수 × 1~2 로 분량이 정해진다. 약점 10개면 10~20문항.

→ **약점 칸 클릭 → 과제 만들기 → 기간·분량만 정하면 끝.** 문제를 직접 안 고른다.
   우리 `출제` 링크는 아직 "검색 결과를 사람이 골라 담는" 단계까지다.

---

## 6. 남은 조사

- [ ] `다음` 이후 화면 (문제 고르기 → 배포)
- [ ] 출제방식 `추천유형` / `선택유형` 의 실제 차이
- [ ] `AI 유형분석` (우상단) — 예측을 새로 돌리는 버튼으로 보임
- [ ] `수업모드` 토글이 무엇을 바꾸는지
- [ ] 서술형 / 고난도 / 단원분석 / 유형이력 탭
- [ ] 주간 클리닉 · 과제 탭 (약점을 **뭘 시켜서** 메꾸는지)
