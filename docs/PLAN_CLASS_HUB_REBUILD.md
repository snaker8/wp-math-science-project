# 학습 운영 라인 재설계 — 반 허브 중심 (매쓰홀릭 구조)

> 작성 2026-09-02 · 근거: `docs/benchmark/matholic/01~08` 실측 + 운영 DB 실측
> 대표 지시: *"이게 다 정리하고 매쓰홀릭식으로 가야한다"* / *"기존 만든거는 신경쓰지말고"*

---

## 0. 무엇을 건드리고 무엇을 안 건드리나

| | |
|---|---|
| **안 건드림** | 자산화(HWP/HML/OCR) · 문제은행 · 인쇄(ExamPaperView) · 시험지 출제 검색 |
| **다시 만듦** | 그 위의 **학습 운영 층** — 반·과제·채점·숙달·리포트 |

자산화와 인쇄는 이 프로그램에서 제일 오래 다듬은 부분이고 잘 돈다. 그건 **자산**이다.
문제는 그 위에 얹힌 운영 층이 **필요할 때마다 하나씩 붙여** 만들어졌다는 것.

---

## 1. 지금 무엇이 잘못됐나 (실측)

### (1) 학생 하나를 보려면 메뉴 6개를 돌아야 한다

```
반 관리 · 학생 관리 · 채점하기 · 수동 채점 입력 · 학생 진단 · 학생 성적
· 클리닉시험지 · 학교별 리포트 · 진단 종합 리포트 · 수업 홈
```

같은 학생 이야기가 열 군데에 흩어져 있다. 매쓰홀릭은 **반 하나를 열면 그 안에 전부** 있다.

### (2) 채점 라인이 두 개다

| 라인 | 저장 | 쓰는 화면 |
|---|---|---|
| A | `diagnostics.sessions` + `items` | 수동 입력 |
| B | `diagnostics.print_sessions` + `session_results` | QR · 이미지 채점 |

**같은 사실("이 학생이 이 문제를 맞았나")을 두 곳에 다르게 쓴다.**
그래서 리포트마다 한쪽만 보이고, 집계할 때마다 합치는 코드를 또 짠다.
(마이그레이션 `20260605_001` 이 숙달 계산만 겨우 합쳐 놨다 — 근본 해결이 아니다)

### (3) "과제" 개념이 없다

우리에겐 **시험지**만 있다. 매쓰홀릭은 시험지와 별개로 **과제**가 있고, 그게 운영의 단위다.
- 기간(시작~마감)이 있다
- 대상 학생이 있다
- 제출/미제출이 보인다
- **네 갈래로 만든다**: 단원 · 오답 · 취약 · 유형

우리는 시험지를 만들어 인쇄해 주면 끝이라, **누가 언제까지 뭘 해야 하는지**가 시스템에 없다.

### (4) 진단 스키마가 API에 안 열려 있다

`diagnostics` 가 PostgREST 노출 목록에 없어 **채점 라인 전체가 런타임에 죽는다.**
아직 안 써서 드러나지 않았을 뿐. 이걸 켜지 않으면 무엇을 만들어도 안 돈다.

---

## 2. 목표 구조 — 반 하나가 스파인

```
반 목록
 └ 반 열기  ──────────────────────────────────────────────
    학생 | 과제 | 채점 | 숙달 | 이력 | 설정
      │      │      │      │      │
      │      │      │      │      └ 주차별 추이 (스냅샷)
      │      │      │      └ 단원 × 난이도 매트릭스
      │      │      └ QR·수동 한 라인
      │      └ 단원 / 오답 / 취약 / 유형  4갈래
      └ 진행도 · 정답률 · 최근 학습
```

매쓰홀릭은 16탭이지만 **우리는 6탭이면 된다.** 레벨업·연산·메쓰톡·문자 같은 건 우리 사업에 없다.

### 왜 반 중심인가
학원은 **반 단위로 운영한다.** "3학년 A반 오늘 뭐 하지"가 실제 질문이고,
"김태한 학생의 확률 숙달도"는 그 다음 질문이다. 지금 우리 IA 는 순서가 거꾸로다.

---

## 3. 데이터 — 세 가지만 정리하면 된다

### (1) 채점을 한 라인으로

**남길 것: `print_sessions` + `session_results`** (B라인)
- `problem_id` 로 실제 문제를 가리킨다 → 오답·취약 과제의 재료가 된다
- QR·이미지 채점이 이미 여기 쓴다 (엑셀 채점은 2026-09-02 제거 — 대표 판단)

**흡수할 것: `sessions` + `items`** (A라인)
- `mathsecr_code` 만 있고 `problem_id` 가 없다 → "무슨 문제를 틀렸는지" 모른다
- 수동 입력 화면을 B라인에 쓰도록 바꾸면 A라인은 읽기 전용 과거 기록이 된다

> ★ 기존 데이터는 **지우지 않는다.** 읽는 경로만 B로 통일하고, A는 과거 이력으로 남긴다.
>   (items 2,746건 · session_results 1,658건 — 둘 다 살아있는 기록이다)

### (2) 과제 테이블 신설

```
assignments            id · institute_id · class_id · title
                       kind: unit | wrong | weak | type      ← 네 갈래
                       exam_id (생성된 시험지)
                       starts_at · due_at · created_by
assignment_students    assignment_id · student_id · session_id(채점 세션) · status
```

**과제 = 시험지 + 대상 + 기간.** 시험지는 이미 만들 수 있으니, 감싸는 층만 있으면 된다.

### (3) 숙달 스냅샷

`student_node_status` 는 **현재 상태만** 있어 추이 차트를 못 만든다.
주 1회 스냅샷을 쌓는다.

```
student_node_snapshots  student_id · week_of · alpha_cnt · beta_cnt · gamma_cnt · unknown_cnt
```

행이 작다(학생 × 주). 매쓰홀릭 유형이력의 GREEN/RED/GRAY 꺾은선이 이걸로 나온다.

---

## 4. 단계 — 각 단계가 혼자서도 쓸모 있어야 한다

| 단계 | 내용 | 선행 |
|---|---|---|
| ~~**0**~~ | ~~`diagnostics` 스키마 API 노출~~ | **✅ 2026-09-02 완료** |
| ~~**1**~~ | ~~채점 한 라인으로~~ | **✅ 2026-09-02 완료** |
| ~~**2**~~ | ~~반 허브 껍데기 + 학생 탭~~ | **✅ 2026-09-02 완료** |
| ~~**3**~~ | ~~과제 — 테이블 + 단원과제 + 제출 현황~~ | **✅ 2026-09-03 완료** |
| ~~**4**~~ | ~~취약과제 · 오답과제~~ | **✅ 2026-09-03 완료** |
| **5** | 숙달 매트릭스 (단원 × 난이도) — 서술형·고난도로 재사용 | 4 |
| **6** | 스냅샷 + 이력 차트 | 5 |

**0번이 없으면 1~6이 전부 무의미하다.**

### ✅ 단계 0 완료 (2026-09-02) — 실측

대표님이 `Exposed schemas` 에 `diagnostics` 추가. 켠 직후 실측:

```
student_node_status  3,950행     session_results  1,658행
print_sessions          96행     sessions            111행
items                2,746행     v_student_mathsecr_heatmap  418행
```

켜지자마자 같이 살아난 것:
- **취약 보충** — 신경욱·최정우 3개월 → 취약유형 18개 · 문제 18개 담김 (실화면 확인)
- **약점 화면**(처방) — 최정우 `중2-1 γ7 β1 α37` / `중3-1 γ11 β1 α49` 표시
  → 같이 넣은 **학생 id 병합 수정**(승격 전 roster id)도 이걸로 검증됐다.

### 이번에 안 하는 것 (명시)
- **AI 예측 숙달도** — 대표 지시로 제외. 안 푼 유형은 "모름"으로 둔다.
- 레벨업 · 연산 · 메쓰톡 · 문자 · 교재비 청구 — 우리 사업에 없다.
- 학생 앱 풀이 화면 — 지금은 인쇄가 주력.

---

## 5. 판단이 필요한 것

| 항목 | 선택지 | 기본 제안 |
|---|---|---|
| 반 허브 위치 | 새 메뉴 / 기존 `수업` 아래 | **기존 `수업` 아래** — 메뉴 안 늘림 |
| 옛 화면 처리 | 즉시 제거 / 당분간 병행 | **병행 후 제거** — 쓰는 사람이 있으면 끊기면 안 됨 |
| A라인 데이터 | 이관 / 읽기전용 보존 | **읽기전용 보존** — 이관은 되돌리기 어렵다 |
| 과제 인쇄 | 시험지와 동일 / 별도 | **동일** — ExamPaperView 재사용 |

---

## 6. 지금 있는 재료 (실측)

| | |
|---|---|
| 문제 | 37,243 (유형 분류 5,563 / 미분류 31,680 — 분류 진행 중) |
| 시험지 | 1,741 · 학교 132개 |
| 숙달 기록 | 63명 · 3,950건 (α 3,282 / β 57 / γ 611) |
| 채점 기록 | session_results 1,658 · items 2,746 |
| 약한 유형 | 251개 중 **244개에서 문제가 뽑힌다 (97%)** |

**취약과제는 지금 데이터로 당장 돈다.** 스키마만 켜지면 된다.

### ✅ 단계 1 완료 (2026-09-02) — 읽는 곳 전수 통일

읽는 경로를 B(print_sessions + session_results) 하나로 모았다. 실측 효과:

| | 전 | 후 |
|---|---|---|
| 보이는 학생 | 44명 | **57명** |
| 보이는 채점 세션 | 111개 | **181개** |

옮기다 드러난 것 (전부 이미 나 있던 사고):
- **부분점수 유실** — 이관 때 `partial:획득/만점` 이 하나도 안 넘어왔다. 실제 부분점수 258문항이
  만점/0점으로 뭉개져 있었다. A 원본에서 `awarded_points`/`max_points` 로 복구(2,721건 전수 일치).
- **자사관 난이도 집계가 항상 0** — A `items.difficulty` 가 2,746건 전부 NULL 이었다.
  이제 `classifications.difficulty`(1~10) 로 계산. 버킷 상 7~10 / 중 5~6 / 하 1~4.
- **학생 위키 이력 중복** — A·B 를 둘 다 찍어 같은 시험이 두 줄씩(110줄) 나왔다.
- **새 공유 링크 미리보기 폴백** — OG 레이아웃이 옛 토큰만 봐서 제목·점수가 안 떴다.

일부러 안 옮긴 것: **담임 수동 진단 입력 폼**(`prescription/lib/queries.ts`).
A 세션 111개가 **전부 EX(시험 채점)** 이라 이 폼으로 만든 세션은 0건 — 죽은 경로다.
쓰기 시작하면 리포트에 안 나오므로, 그때 B 스키마로 옮긴다(코드에 경고 주석).

### ✅ 단계 2 완료 (2026-09-02) — 반 허브 + 학생 탭

- `GET /api/classes/[classId]/hub` — 반 + 학생별 (회차 · 채점 문항 · 정답률 · 최근 학습 · α/γ)
- `/dashboard/class/[classId]` — 탭 6개(학생·과제·채점·숙달·이력·설정), 학생 탭만 채움
- 반 목록(`/tutor/classes`) 카드에 **「반 열기」** 추가. 기존 「등록 관리」는 옆에 남김
- 트랙 스텁 `(tracks)/[track]/dashboard/class/[classId]` 같이 생성 (없으면 트랙 URL 에서 404)

실데이터 확인 — 중3 엄궁중 5명:
`강다현 6회 134문항 84% α87/γ21` · `김채은 7회 152문항 67% α57/γ38` …

빈 탭에는 "무엇이 올 자리인지"를 적어 뒀다. 없는 걸 있는 척하는 것보다 낫다.

### ✅ 단계 3 완료 (2026-09-03) — 과제

- 마이그레이션 `20260903_001_assignments` (prod 적용 완료)
  `assignments` (kind: unit/wrong/weak/type · exam_id · starts_at/due_at)
  `assignment_students` (assignment_id · student_id · session_id · status)
- `GET/POST/DELETE /api/classes/[classId]/assignments`
- 반 허브 「과제」 탭 — 목록 + 펼치면 학생별 제출/정답률, 「과제 내기」 모달

★ 설계에서 가장 중요한 결정: **제출 여부를 assignment_students 에 안 박는다.**
  "이 학생이 이 시험지를 쳤나" 의 사실은 채점 세션에 이미 있다. 같은 사실을 두 곳에
  쓰면 반드시 어긋난다 — 채점 라인이 둘로 갈려 리포트마다 한쪽만 보이던 게 정확히
  그 사고였다. 그래서 제출 현황은 **매번 채점 세션에서 계산**하고, `status` 컬럼은
  면제(excused) 같은 **사람의 판단**만 적는다.

실데이터 검증 (중3 엄궁중 5명):
- `25-3-1-F 엄궁중 수학` → 5/5 제출, 정답률 50·65·65·85·90 (평균 71%)
- `25-3-1-F 엄궁중 수학 유사1` → **4/5 제출, 1명 미제출**, 평균 73%
검증용 과제는 확인 후 삭제.

**학생 id 에 FK 를 안 걸었다.** users.id 일 수도 roster_students.id 일 수도 있어서
(명단으로 채점하다 승격하면 기록이 옛 id 에 남는다) 두 테이블을 동시에 가리키는
FK 는 만들 수 없다. 앱의 신원 병합으로 푼다.

### ✅ 단계 4 완료 (2026-09-03) — 취약 과제 · 오답 과제

- `POST /api/clinic/wrong-problems` (신규) — 학생들이 **실제로 틀린 문제** 모으기.
  (학생, 문제)별 **마지막 채점만** 본다 — 그 뒤에 맞혔으면 오답이 아니다.
  여러 명이 틀린 문제를 앞에 둔다(반 과제로는 그게 제일 값싼 한 방).
- `POST /api/classes/[classId]/assignments/from-problems` — 고른 문제 → 시험지 → 과제를 한 번에.
  중간에 실패하면 **되돌린다**(빈 시험지·대상 없는 과제 금지).
- 반 허브 과제 탭에 「취약 과제」·「오답 과제」 버튼 + 생성 모달.

★ 흐름을 거꾸로 잡았다 — **결과가 선택된 채로 오고 교사는 빼기만 한다.**
  고를 게 수십 개인데 하나씩 담게 하면 아무도 안 쓴다.

★ 다만 **상한을 뒀다.** 실측하니 중3 5명 한 반에 약한 유형이 **82개**(γ74·β8)였다.
  다 담으면 80문항짜리 과제가 나가는데 그건 아무도 안 푼다 → 앞에서부터 20문항만
  담고 나머지는 보여만 준다.

실데이터 (중3 엄궁중 5명):
- 취약: γ74 + β8 = 82유형, 그중 **73유형에서 문제가 뽑힌다(89%)**
- 오답: **91문항**, 그중 **40문항은 2명 이상 공통 오답**, 최대 4명이 틀린 문제

### ✅ 단계 5 완료 (2026-09-04) — 숙달 매트릭스 (매쓰홀릭 유형분석 대응)

대표 지시(2026-09-04): *"우리 거에 매쓰홀릭을 맞추지 말고, 없으면 기능을 만들어 넣어서 매쓰홀릭 수준까지."*
→ 우리 데이터 한계에 맞춰 화면을 깎지 않았다. 매쓰홀릭 유형분석(08 §1~5)을 기준으로 두고 없는 것은 만들었다.

- `GET /api/classes/[classId]/mastery` (신규) — 재료를 그대로 준다: 채점 문항(학생·유형코드·난이도·정오·시각) ·
  수학비서 트리 이름 · **문제은행 공급량**(소단원 × 난이도, 격리·트랙 통과분만). 접는 축·기간·학생·추정은 클라 토글.
  같은 재료로 단계 6 「이력」이 graded_at 누적으로 그려진다 → **스냅샷 테이블 없이 간다** (3-(3) 계획 폐기).
- `POST /api/clinic/cell-problems` (신규) — 고른 칸(소단원 × 난이도)마다 새 문제 N개. 이미 푼 문제 제외, 세부유형 분산.
- `src/lib/class/mastery-bands.ts` — 4단계/6단계 밴드 · 칸 판정 (vitest 18건).
- `src/lib/class/mastery-infer.ts` — **● 추정 칸** (매쓰홀릭 「AI 예측」 대응). LLM 없이 규칙 3개(같은 소단원 상하 밴드 ·
  같은 중단원 형제 평균 · 1~2문항은 사전분포와 섞기). 모든 추정 칸은 근거 문장을 들고 다닌다. 비용 0.
  **★ 기본 꺼짐** — 대표 판단(2026-09-04): *"AI 예측을 원치 않는 게 아니라, 우리 학생들의 유형 히트맵을 완성하고 난 뒤
  가야 할 기능."* 순서: ① 실제 채점으로 히트맵 완성(분류 커버리지↑·채점 회차↑) → ② 예측. 토글로 켜면 보인다.
- `src/lib/class/class-students.ts` — 반 학생 + 신원 병합을 **한 곳**으로 (hub · mastery · cell-problems 공용).
- `MasteryMatrix.tsx` — 매쓰홀릭 화면 구성 그대로: 행=단원 · 열=난이도(4/6단계) · 칸=유형 · 색=숙달 · 우측 학생 선택 ·
  칸/행/열 덩어리 선택 · 「선택된 칸 N + 과제 만들기」 · 범례 실측 카운트 · 기간 · 칸 툴팁 + 대표 문제.
- `GenerateAssignmentModal` — 매쓰홀릭 과제 옵션(08 §5-1): 과제명 자동 제안 3종 · 마지막 마감 재사용 · 칸당 1/2/3문제.

**★ 판의 원리 — 최종 (대표 2026-09-04 밤)**: *"우리 유형분석 판은 수학비서 코드로 분류된 유형별로 만들고,
거기에 난이도별로 문제들이 적층되어, 학생들이 그걸 풀었을 때 완성도 색이 변하는"* 것.
| | |
|---|---|
| 판 | 과정(과목)의 수학비서 **유형(depth5)** 전체. 과정마다 고정. 채점의 산출물이 아니다 |
| 칸 | 유형 하나. 칸 안에 개념→기본→실력→심화 층이 세로로 쌓인다 (아래가 개념) |
| 층 | 그 유형·난이도의 문제은행 문제. 없으면 점선(문제 없음) · 있는데 안 풀면 회색(미학습) · 풀면 색 |
| 색 | 색조=정답률(초록/노랑/빨강) · 진하기=푼 비율(푼 문제/있는 문제) |
| 줄 | 소단원(depth4). 위로 중단원·대단원 묶음 + 대단원 필터 칩 |
(처음엔 "depth5 는 회색 밭이 된다"며 칸을 depth4×밴드로 올렸는데 — **틀렸다.** 판이 듬성한 건 판의 잘못이 아니라
문제은행 완성도의 현재 값이고, 대표: *"당연히 문제량이 작으니. 실제 고등부 자료가 많으니까."*)
depth4 코드로만 분류된 문제(중3-1 201건)는 그 소단원의 「(유형 미지정)」 칸으로 — 판에서 사라지면 안 된다.

**★ 두 가지 정직 장치** — 흉내가 되는 지점을 실측으로 막았다:
1. **1~2문항엔 색을 안 준다** (판정 보류). `student_node_status` 3,950건 중 **80%가 items_total=1** — 한 문제로
   마스터/약점을 칠하면 소음이 신호처럼 보인다. 매쓰홀릭에도 「판정 불가」가 있다.
2. **난이도 밴드 경계를 실측 분포로** — 분류 7,369건이 1~3: 3,801 · 4~5: 2,493 · 6~7: 926 · 8~10: 149.
   매쓰홀릭식 9~10을 「심화」로 자르면 21문제뿐이라 열이 통째로 빈다.

실데이터 (중3 엄궁중 5명, 중3-1, 2026-09-04): **판 1,732유형**(depth5 1,657 + 유형 미지정 75) ·
**문제은행 완성도 672/1,732 (39%)** · **반 진행도 175/1,983문제 (9%)** · 색 붙은 칸 105 · 층 2개 이상 쌓인 유형 143.
층별 공급: 개념 369 · 기본 329 · 실력 99 · 심화 8 유형. **분류가 21%(7,369/34,980)뿐**이라 이 숫자는 분류가 붙을수록
올라간다 (헤르메스 분류 재개 대기).

매쓰홀릭에 있고 아직 없는 것: 교재별 매트릭스 전환(교재 유형 자료 없음) · 서술형/고난도 탭(문제 형식 컬럼 없음 —
`answer_json` 에서 파생 필요) · 9주 이력 차트(단계 6, 같은 재료로).

**★ 판의 정의 정정 (2026-09-04 밤, 대표)**: *"채점 데이터로 히트맵이 아니다. 모든 과정마다의 히트맵이 있다."*
처음 구현은 「문제은행에 문제가 있는 칸」만 판에 올려 분류 21% 만큼의 판이 됐다 — 틀렸다.
판은 **과정(과목)의 수학비서 트리 전체(소단원 × 밴드)로 고정**이고 채점은 색칠일 뿐. 문제 없는 칸도 판에 있고
「문제 없음」(점선)으로 「미학습」(회색)과 구분한다. 그래야 완성도가 두 개로 갈린다:
- **문제은행 완성도** = 문제 있는 칸 / 판 전체 — 중3-1 실측 **429 / 1,362 (31%)** (6단계 기준)
- **학습 진행도** = 학생이 푼 칸 / 문제 있는 칸 — 중3 엄궁중 반 전체 **74 / 429 (17%)**
→ 예측(추정)은 이 판이 채워진 뒤에 갈 기능이라 **기본 꺼짐**.
→ 매쓰홀릭의 완성도·진행도(학생 탭 "90% 46개/51개")·수업 목록 과정별 미니 히트맵은 **정의를 안 봤다. 2차 조사 1순위.**

### ✅ 단계 8 (부분) 완료 (2026-09-04) — 설정 탭: 학습 목표 · 반 정보 · 학생 등록 + 학생 탭 달성률

매쓰홀릭 프리미엄 2차 조사(`docs/benchmark/matholic/10-course-hub-premium-deep.md` §0)에서 확인한 뼈대 중
**「학습 목표」** 를 먼저 넣었다 — 이게 있어야 학생 탭의 달성률·날씨가 생긴다. 비용 0.

- `classes.settings.goals = { weeklyProblems, accuracy }` — 컬럼 추가 없이 jsonb 에. `PUT /api/classes/[id] { goals }` 검증(10~500 / 50~100).
- `lib/class/learning-goals.ts` — 목표 파싱·검증 · 달성률(100 초과 그대로, 매쓰홀릭 176%) · 날씨(≥100 맑음 / 50~99 구름 조금 / <50 흐림 — 경계는 실측 3점에 맞춘 **미검증**) · **KST 월요일 주 경계** (vitest 8건 — 일요일 밤 KST 가 UTC 로 다음 주로 튀는 함정).
- `GET /api/classes/[id]/hub` 확장 — 학생별 `진행도(제출 과제/배정 과제) · 금주 학습량 · 금주 정답률 · 평균 학습량(문항/학습한 주) · 평균 정답률` + 목표 대비 달성률 4종. **목표가 없으면 달성률·날씨를 만들지 않는다** — 목표 없이 "0% 달성"은 거짓.
- `SettingsTab.tsx` — 학습 목표 · 반 이름/설명 · 담당 강사(표시) · 학생 등록 목록/제거(추가는 기존 `/tutor/classes/[id]` 로 링크).
- 학생 탭 칼럼: `학생 · 진행도 · 금주 학습량 · 금주 정답률 · 평균 학습량 · 평균 정답률 · 숙달/취약 · 최근 학습` (매쓰홀릭 학생 탭과 같은 줄).

실데이터 (중3 엄궁중 5명, 목표 70문항/80점 임시 설정 후 되돌림): 강다현 평균 학습량 64%(45/70) · 평균 정답률 105%(84/80),
금주는 전원 0개(방학·채점 없음). 진행도는 이 반에 과제가 없어 「—」.

**매쓰홀릭과 다른 점(정직하게)**: 매쓰홀릭 진행도 = 완료 회차/전체 회차(수업=교재 51회차). 우리는 회차 층이 없어 **과제 제출/배정** 으로 대신한다.
회차(코스) 층은 별도 설계 — 10 문서 §4.

아직 없는 것(설정 탭): 수업 일정(요일) · 담당 변경 · 완료/복구 · 출제 방식(개인화/공통) · 학생 추가를 탭 안에서(지금은 옛 등록 화면 링크).
