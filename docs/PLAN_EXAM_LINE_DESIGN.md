# 출제 라인 통합 설계서 (2026-08-30)

> 근거: `docs/benchmark/matholic/01~07` (매쓰홀릭 실계정 라이브 실측) + 우리 DB 실측
> 상위 문서: `docs/PLAN_EXAM_LINE.md` (목표 루프·현재 위치)
> ★ 원칙: 자산화·인쇄 라인은 건드리지 않는다. 기존 화면은 즉시 삭제하지 않는다.

---

## 0. 한 줄 결론

**출제 진입을 하나로 만들고(`/dashboard/exam/new`), 소스를 고른 뒤 3단계로 흐르게 한다.
그 끝은 이미 완성된 우리 인쇄 라인(ExamPaperView)에 그대로 인계한다.**

---

## 1. 목표 화면 구조

```
[출제] 메뉴 → "새 시험지 만들기" (진입 하나)
        ↓
❶ 소스 선택 — "무엇을 근거로 뽑을까"
        ↓
❷ 문제 선택 — 4열: 과목/과정 → 단원트리 → 유형목록 → 출제 구성(★난이도 분포 카운터)
        ↓
❸ 문제 편집 — 좌: 시험지 배열 / 우: 후보풀(유사·고난도·서술형·기출) [교체][추가]
        ↓
❹ 시험지 설정 — 제목·기간·분할 → 저장
        ↓
[기존 인쇄 라인] exam-management / ExamPaperView (2단·배점·한글·헤더) — 무변경
```

## 2. ❶ 소스 선택 — 우리 강점을 첫 칸에

매쓰홀릭은 13종(교재연계 3·내신수능 6·초등 1·문제은행 2·목적별 4).
우리는 **가진 것 + 강점 순서**로 6종에서 시작한다.

| 순 | 소스 | 근거 데이터 | 상태 |
|---|---|---|---|
| **1** | **우리 학원 기출** ★강점 | 학교기출 141건, 출처 100%(학교·연도·중간/기말) | 즉시 가능 |
| 2 | 유형별 | 수학비서 2,573 유형 + 분류 5,897건 | 즉시 가능 |
| 3 | 단원별 | 같은 분류 데이터 | 즉시 가능 |
| 4 | **오답(학생별)** ★루프 | 진단 items 2,746 + QR 1,658 + 숙달 3,950 | 즉시 가능 |
| 5 | 고난도·서술형 | difficulty + problems.type | 즉시 가능 |
| 6 | 시중교재 유사 | 시중교재 자산화 1건 → **축적 대상** | 데이터 쌓이는 만큼 |

★ 매쓰홀릭에 없고 우리에게 있는 것: **"그 학교 기출로 그 학생을 추적"**.
   매쓰홀릭은 전국 10만 DB(넓이), 우리는 우리 학원 학교 기출(정밀).
   → 소스 1번을 **학교·학년·학기·중간/기말 4단 좁히기**로 만든다. 우리만의 첫 화면.

## 3. ❷ 문제 선택 — 4열 + 난이도 분포 카운터

참조: `benchmark/matholic/02-problem-select.md`

| 열 | 내용 | 우리 데이터 |
|---|---|---|
| 1 | 과목·과정 (중1-1 … 미적분) | curriculum_codes / mathsecr 과목 |
| 2 | 단원 트리 (대→중단원, 부분선택 ■) | mathsecr_types 계층 |
| 3 | 유형 목록 (소단원 유형 + `>` 개별문항) | mathsecr leaf + problems |
| 4 | **출제 구성 패널** | 아래 |

### 출제 구성 패널 (4열)
| 옵션 | 값 | 우리 구현 |
|---|---|---|
| **★난이도 분포 카운터** | 개념/기본/실력/심화/고난도 5단 + 막대 + 실시간 | classifications.difficulty(1~10) → 5밴드 매핑. **최우선** |
| 유형별 문제수 | 1/2/3/4개 | 유형 × N 배분 |
| 답안 형태 | 전체/객관식/주관식 | problems.type |
| 최대 문제수 | 프리셋+슬라이더 | 상한 컷 |
| 중복 출제 방지 | on/off | 학생 이력(session_results) 대조 — 2차 |
| 대표문제 우선 | on/off | usage_count 기준 — 2차 |

★★ **난이도 분포 카운터가 이 화면의 심장**이다. 유형을 고르는 즉시 구성이 보이고
   편중되면 바로 조정 — 이것이 "탁탁 자유롭게"의 실체 (실측: 개념14/기본14/실력22/심화18/고난도0).

## 4. ❸ 문제 편집 — 2분할 + 교체/추가

참조: `benchmark/matholic/03-problem-edit.md`

- **좌**: 선택 문제 번호순 배열(난이도 배지·삭제·정렬). 우리 `SelectionTray` 확장
- **우**: 후보풀 탭 — `유사(기본)` / `기출` / `고난도` / `서술형` / `내 폴더`
- 각 후보에 **[교체]**(번호 유지·대체) / **[추가]**(새 문항) **두 버튼 분리** ← 우리에 없는 개념
- 유사 후보 = **①같은 유형 다른 문제(DB) + ②twin-generator 변형(숫자·조건)**
  → AI 변형은 비용 발생 → 해설 PIN 게이트와 같은 방식으로 통제 검토

## 5. ❹ 시험지 설정 → 인쇄 라인 인계

- 제목(자동 제안: 범위+과정+시각), **기간(제출기간)**, 서술형 시작번호, **분할생성(N문제씩)**
- 저장 → exams 레코드 생성 → **기존 exam-management/ExamPaperView 로 이동**
- ★ 인쇄·조판·한글 출력은 **손대지 않는다** (이미 우리가 더 강함)

## 6. 기존 6화면 처리

| 화면 | 처분 | 시점 |
|---|---|---|
| `/dashboard/exam-create` (2,163줄) | **새 화면의 모체로 흡수** — 소스탭·필터·SelectionTray 재사용 | 1차 |
| `/dashboard/create` (1,646줄) | 단원·유형 트리 로직만 흡수 후 새 화면으로 리다이렉트 | 2차 |
| `/dashboard/similar` (711줄) | ❸ 후보풀 "유사" 탭으로 흡수 | 2차 |
| `/dashboard/curation` (279줄) | ❷ "자동 배분" 옵션으로 흡수 | 3차 |
| `/dashboard/cloud/create-exam` (346줄) | 클라우드에서 담아온 문제로 ❸ 진입하는 경로로 유지 | 유지 |
| `/dashboard/exam-management` (1,562줄) | **유지** — 저장 후 인쇄·배포 라인 | 무변경 |

★ 라우트는 남기고 리다이렉트. 새 화면이 안정된 뒤 정리 (운영 지장 0).

## 7. 구현 순서

| 단계 | 내용 | 산출 |
|---|---|---|
| **S1** | ❷ 문제 선택 + **난이도 분포 카운터** | 체감 최대. 데이터 이미 보유, AI 비용 0 |
| **S2** | ❶ 소스 선택 (우리 학원 기출 첫 칸) + ❹ 저장→인쇄 인계 | 한 흐름 완주 |
| **S3** | ❸ 문제 편집 2분할 + 교체/추가 + 유사 후보(DB) | 편집 자유도 |
| **S4** | 오답 소스 (학생 선택 → 기간 → 오답원본/유사 N) | ★메꾸기 루프 진입 |
| **S5** | twin-generator 연결 (AI 변형 후보) ★비용 | PIN 게이트 검토 |
| **S6** | 유형 숙달 매트릭스 (진단 3,950건 시각화) → 약점→출제 원클릭 | 루프 완성 |

## 8. 이번에 하지 않는 것 (명시)

- 인쇄·조판·한글 출력 (완성됨)
- 자산화 파이프라인 (완성됨)
- 반 허브 16탭 (별도 과제 — `benchmark/06`)
- 날씨 게이미피케이션·주간 목표 (별도 과제)
- 학생 풀이 화면 (별도 과제 — `/student/solve/[id]` 12줄 껍데기)

## 9. 검증 기준 (각 단계 배포 전)

1. `npx tsc --noEmit` 통과
2. `npm run build` 통과
3. `npm run design:guard` 증가 없음
4. 로그인 상태 dev 육안 확인 (실데이터)
5. 기존 자산화·인쇄 흐름 회귀 없음

---

## 10. 적용 기록

### S1 난이도 분포 카운터 (2026-09-07)

| 항목 | 결과 |
|---|---|
| 밴드 | `lib/class/mastery-bands` 에 **5단** 추가 — 개념 1~3 · 기본 4~5 · 실력 6~7 · 심화 8~9 · 고난도 10. 판(4단)의 심화를 한 칸 더 쪼갠 것이라 **출제와 판이 같은 말**을 쓴다. vitest 24 |
| 카운터 | `DifficultyDistribution` 를 1~10 막대 → **5단 행(라벨·막대·개수·%)** 으로. 매쓰홀릭 실측(개념14/기본14/실력22/심화18/고난도0)과 같은 형태 |
| 미분류 | 난이도 없는 문제를 **숨기지 않고 「미분류 N」으로 센다** — 분류 23% 진행 중이라 섞여 들어온다. 숨기면 분포가 사실보다 고르게 보인다 |
| 접힌 상태 | 선택 트레이 탭에 **5단 미니 막대** — 트레이를 열지 않아도 편중이 보인다 |

**남은 것 (S1 의 나머지 = 출제 구성 패널)** — 유형별 문제수(1~4개) · 답안 형태(전체/객관식/주관식) · 최대 문제수 · 중복 출제 방지 · 대표문제 우선. 카운터는 「보여주는」 절반이고 이쪽이 「맞추는」 절반이다.

**그 다음** — S2 통합 진입(`/dashboard/exam/new`, 지금 출제 화면이 6개로 흩어져 있다) · S3 2분할 편집(교체/추가) · S4 오답 소스 · S5 twin-generator 연결(AI 비용).

### 난이도 언어 통일 (2026-09-12) — 출제 라인의 1~5 척도 버그

**증상**: `/dashboard/create`(유형별 출제)가 최상·상·중·하·최하 의 **1~5 척도**였다.
우리 `classifications.difficulty` 는 **1~10** 이라, 실측 기준 **난이도 6~10 의 2,862문항(전체 분류의 28%)**이
① 배지에서 전부 '중' 으로 찍히고 ② 난이도 필터로 못 고르고 ③ 자동 배분 풀에서 아예 안 뽑혔다.

| 곳 | 전 | 후 |
|---|---|---|
| `lib/class/mastery-bands` | 5단 밴드만 있음 | `EXAM_BAND_LABELS` · `LEVELS_BY_BAND_LABEL` · `bandLabelOf()` — 출제와 판을 잇는 다리 |
| `/api/exams/available-counts` | `{'1'..'5'}` 카운트 | 5단 라벨 카운트 (`bandLabelOf`) |
| `/api/exams/generate` | `'최상'→'5'` 단일값 | 라벨 → 레벨 **묶음**, 풀은 그 범위 전체 |
| `/api/problems/search` | `difficulty` 단일 `.eq()` | `band` 파라미터 → `.in(levels)` (`difficulty` 는 백워드 호환) |
| `/dashboard/create` | 최상~최하 · 배지 1~5표 | 개념·기본·실력·심화·고난도 · 배지 `bandLabelOf(1~10)` · 미분류 표시 |

이제 출제 화면 · 유형분석 판 · 코스 회차 계단이 **같은 다섯 단어**를 쓴다. vitest 28.

### S1 출제 구성 패널 (2026-09-12)

난이도 카운터의 「보여주는」 절반에 이어 「맞추는」 절반.

| 옵션 | 구현 |
|---|---|
| **답안 형태** (전체·객관식·주관식) | `problems.answer_type` 생성 컬럼. 자동 모드 세그먼트 · 수동 검색 select. `available-counts`·`generate`·`problems/search` 세 곳 모두 필터 — **고르면 밴드별 「있는 문제 수」도 그 형태로 다시 센다**(화면 숫자와 실제로 뽑히는 문제가 어긋나면 안 된다) |
| **최대 문제수** | 10/20/25/30/50 프리셋(기본 30). 예전엔 50 이 코드에 박혀 있었다. 합계가 상한을 넘으면 호박색 경고 |

**남은 것** — 중복 출제 방지(학생 이력 대조)·대표문제 우선(usage_count)은 2차. 그 다음이 S2 통합 진입.

### ✅ 해결: 훅 순서 결함 + 과학 트랙 잠금 (2026-09-12, 대표 「과학 일단 비활성화」)

`/dashboard/create`(39건)·`/dashboard/problem-bank`(27건)이 과학 트랙 안내 카드를 **훅 선언보다 위에서 조기 return** 했다.
화면에 머문 채 트랙을 전환하면 React 가 훅 개수 불일치로 터지는 구조였다. **eslint rules-of-hooks 66건 → 0건.**

| 조치 | 내용 |
|---|---|
| 과학 잠금 | `featureFlags.SCIENCE_TRACK_DISABLED = true` → `TRACK_SPLIT_ENABLED` 가 항상 false. env 는 그대로 두어 **실수로 켜지는 것도 막는다**. 다시 켤 때 이 상수만 false |
| 스위치 일원화 | 미들웨어가 `process.env` 를 직접 읽어 클라이언트와 갈려 있었다 → `featureFlags` import 로 통일 |
| 조기 return 제거 | 두 화면에서 과학 안내 블록 삭제(잠갔으니 분기 자체가 불필요) + 미사용 `Beaker` import 정리 |

**URL 호환**: `/math/dashboard/...` 는 `(tracks)/[track]` 라우트 그룹이 그대로 있어 계속 열린다. `/dashboard/...` 직접 주소도 동작.
플래그 문서가 말하는 대로 false 가 「모든 사용자 math 단일 트랙 = 기존 운영 흐름」이다.

### S2 통합 진입 (2026-09-13)

`/dashboard/exam/new` — 「무엇을 근거로 뽑을까」를 먼저 고르는 한 칸. 출제 메뉴의 첫 항목.

| 순 | 카드 | 가는 곳 | 실측 숫자 |
|---|---|---|---|
| **1** | **우리 학원 기출** ★강점 | `exam-create?source=school` | 시험지 N장 · N개교 |
| 2 | 유형별 · 단원별 | `/dashboard/create` | 유형 붙은 문제 N / 전체 N |
| 3 | 취약 보충(학생별) | `exam-create?source=weak` | 반 N개 |
| 4 | 시중교재 | `exam-create?source=textbook` | 교재 묶음 N |
| 5 | 모의고사 | `exam-create?source=mock` | — |
| 6 | 진단평가 | `exam-create?source=diagnostic` | — |

- `GET /api/exams/source-summary` — 격리 필터를 통과한 것만, **확실한 것만** 센다(애매한 집계를 그럴듯한 숫자로 보여주지 않는다).
- `exam-create` 가 `?source=` 를 읽어 탭을 미리 연다 (`useSearchParams` 대신 mount 1회 — Suspense/CSR 폴백 회피, 기존 `?typeCode=` 와 같은 방식).
- **기존 화면·라우트는 그대로 둔다.** 설계서 §6 대로 새 화면이 자리 잡은 뒤 정리한다 (운영 지장 0).

**남은 것** — S3 2분할 편집(교체/추가) · S4 오답 소스 · S5 유사문제 생성기 연결.

### S3 문제 편집 2분할 — 교체 / 추가 (2026-09-13)

매쓰홀릭에 있고 우리에게 없던 개념: **[교체]와 [추가]를 나눈다.**
한 버튼으로 뭉쳐 두면 「이걸 누르면 원래 문제가 빠지나?」를 매번 헷갈린다.
교체는 그 **자리를 대신**한다(번호 유지), 추가는 맨 뒤에 새 문항으로 붙는다.

| 조각 | 내용 |
|---|---|
| `GET /api/problems/candidates` | `?problemId=&kind=&exclude=` — 기준 문항의 유형(depth5)으로 후보를 찾는다. 격리·트랙 통과분만, 이미 담은 문제는 제외 |
| 탭 4종 | **유사**(같은 유형 · 난이도 가까운 순) · **기출**(그 유형의 학교기출) · **고난도**(기준보다 높은 난이도) · **서술형**(`answer_type=short_answer`) |
| `CandidatePool` | 후보 카드에 밴드 배지·학교·연도·본문 미리보기 + [교체][추가] 두 버튼 |
| `SelectionTray` | 담은 문항의 ⟳ 버튼 → 트레이가 380px → 740px **2분할**로 열린다 |

- **AI 안 쓴다.** 전부 우리 분류·출처로 찾는다 (AI 변형 후보는 S5, 비용 별건).
- 유형이 없는 문제(미분류)는 후보를 못 찾는다 — 빈 목록 대신 **이유를 적어 보여준다**(「분류 대기」).
- 실측(MS07-05-03-11-01, 난이도 5 기준): 유사 65 · 기출 65 · 고난도 51 · 서술형 22.

**남은 것** — S4 오답 소스 · S5 유사문제 생성기(AI) 연결.
