# HANDOFF 2026-06-16 (세션 인계)

> 컨텍스트 가득 참. 아래 "★ 열린 스레드" 부터 보면 됨. 이번 세션은 PR #325~#348 머지 + HML 가져오기(미머지 브랜치) 진행.

---

## ★★ 지금 열린 스레드 — 한글 .hml 직접 가져오기 (브랜치 `claude/hml-import`, **미머지/미배포**)

### 배경 / 결정
- 사용자 자료 = **수학비서가 내보낸 `.hml`(HWP ML, UTF-8 XML)**. (`.hwp/.hwpx` 아님)
- 기존 HWP→PDF 변환은 **LibreOffice 의존 → Vercel 서버리스엔 없음 → 배포에서 실패**(로컬만 됨). `convertHWPtoPDF` (`src/lib/workflow/hwp-converter.ts`).
- 사용자: "PDF로 저장하면 누락 생긴다" → **OCR 안 거치는 .hml 직접 파싱**으로 결정. 저장은 **(B) 검수화면 없이 바로 클라우드 시험지** + 펼쳐보기에서 수정. **AI 분류 없음(비용 0)**.

### 만든 것 (브랜치 `claude/hml-import`, 커밋 a4fdc69, push 됨, **main 머지 안 함**)
- `src/lib/workflow/hml-parser.ts` — HML→문제 추출. fast-xml-parser `preserveOrder`. **EQUATION 은 `<SCRIPT>` 직계만**(SHAPEOBJECT>CAPTION 에 렌더이미지 base64 섞여 있어 버려야 함). 문제 구분 = **`[정답] ⊙` 헤더**(정답 인라인 내장!). 보기 ①~⑤ split. base64 누출/`수학비서` 워터마크 제거(`cleanText`).
- `src/lib/workflow/hangul-equation.ts` — 한글수식→LaTeX. `left/right/sqrt/over`+그리스 등 백슬래시 보정, `{a} over {b}`→`\frac`. (KaTeX 가 `\over` 도 지원해 미변환도 렌더됨)
- `src/lib/workflow/hml-save.ts` — `createExamFromHml(supabase, parsed, ctx)` 주입형. 자산화 가드 반영(같은 title+institute+book_group 중복차단·created_by 필수·institute 격리). 그림 base64→`source-files/hml-imports/{examId}/...` 업로드→`images:[{type:'figure_crop',url}]`.
- `src/app/api/workflow/import-hml/route.ts` — auth(ADMIN/TEACHER/TUTOR/ORG_ADMIN/super) + institute 결정(`isSharedLibraryMode`+`isOrgIsolatedAssets` 자산화와 동일) → parse → createExamFromHml.
- `src/components/workflow/CloudFlowUploader.tsx` — accept 에 `.hml` 추가, 제출 핸들러에 `.hml` 분기(→ import-hml → 클라우드 시험지로 router.push). (line ~189 분기)
- `package.json` — `fast-xml-parser` 추가.

### 검증 완료 (실파일/실DB, 배포 X)
- 신곡중 .hml: **문제 22 · 정답 21 · 보기5지 21 · 그림 6** 추출, 수식 LaTeX 변환 OK.
- `createExamFromHml` 로 **실 Supabase 저장 → 행 확인 → 삭제** 성공. tsc 0 · build exit 0.
- 로컬 dev(localhost:3000)에서 실제 업로드 → **펼쳐보기에 문제·보기·정답·분류·난이도 다 정상 표시 확인**(사용자 스크린샷).

### ★ 남은 문제 (다음 세션 우선)
1. **그림이 깨져 보임** — 펼쳐보기에서 figure_crop 썸네일이 broken(회색 박스). 가설: `source-files` 버킷 public 접근/`proxyUrl` 문제, 또는 업로드 경로 RLS. 기존 자산화 crop 은 잘 뜨므로 **그 저장/렌더 방식과 정확히 맞춰야**(ExamProblemRenderer 는 `figureCrops[i].url` 을 `proxyUrl()` 통해 렌더). hml-save 의 업로드 path/URL 형식을 기존 crop 과 대조할 것.
2. **"원본"이 없어 편집 대조 불가** — HML 은 페이지 스캔 이미지가 없어 펼쳐보기 "원본 보기/사용"이 빈 상태. 사용자 지적: "원본이 안보이니 수정을 못한다". 대안: (a) 원본 없음 UI 안내 + 텍스트 기반 편집으로 충분히 하게, (b) .hml 의 그림만이라도 원본 자리에, (c) 페이지 렌더 생략 수용. **사용자와 방향 합의 필요.**
3. 서답형(동그라미 아닌) 정답은 빈값으로 들어감 → 펼쳐보기 입력. 복잡 수식 일부 펼쳐보기 수정.
4. (해소) 스크린샷의 분류 배지는 **사용자가 펼쳐보기에서 직접 분류한 것**. HML import 는 설계대로 분류 안 함(비용 0) — 정상.

### 다음 행동
- 그림 렌더(1) 먼저 고치고 → 원본 대조(2) 방향 사용자 확인 → 잘 되면 **main 머지(배포)**. 그 전엔 배포 금지(사용자 지시 "검증 후 배포").
- 로컬 dev 서버가 떠 있었음(`npm run dev`, localhost:3000, +yolo 8100 +imgpipe 8200). 새 세션은 필요시 재기동.
- 검증 임시 vitest(`__hml_*verify.test.ts`)는 .env·prod DB 접근이라 **이미 삭제함**(다시 만들면 커밋 금지).

---

## 이번 세션 머지 완료 (전부 prod 반영)
- #325 수업 보고서 탭 리포트 허브화(진단세트 딥링크·개별리포트·공유링크 내역) + report-tokens API
- #328 반관리: 반 수정 실동작(목업 제거)·가입학생 추가(direct enroll)·등록 시 반배정
- #329 개별리포트 학부모공유 QR채점 학생 발급 버그(두 라인) + 마이그 `20260612_001_parent_share_exam_tokens`(exam_id)
- #330 개별리포트 학부모링크 원클릭 발급+복사(세트와 동일)
- #333 nav 그룹 순서(문제은행·수업·출제·채점·분석·학원자료·DB자산화) + 출제관리→출제그룹 이동 + 출제 라벨(유형별/자산 시험지 출제)
- #334 **수업 허브 `/dashboard/class`** (IA Phase2) — StudentTreePanel·StudentLearningReport 공용컴포넌트 추출, 출제관리와 공유
- #335 인쇄 가운데 구분선 페이지 하단까지(페이지 앵커 absolute — 수식과 구조 분리, 회귀 차단) / #336 2단 좌우 균등 minmax(0,1fr) / #337 문제번호 위로+본문 전체폭+글자13.5px / #338 서술형 배점 배지 명령문 뒤(본문 전체 지시어 탐색)
- #339 반관리 관리자 전체 반 표시(역할인지 /api/classes)+활성학원핀+인원 서버집계+삭제 API
- #340 자유조합 진단리포트 학부모링크 '찾을수없음'(examIds 저장·재계산) + 마이그 `20260611_002_parent_share_exam_ids`(exam_ids)
- #341 자유조합 OG 썸네일 examIds / #342 진단 종합리포트 소속을 학원명으로(compute-report.instituteName, display_name>org>name 폴백)
- #343 1차 분석 그림 위치 이동 버튼(맨위/맨아래 — content 마커 재배치)
- #344 **빠른답 객관식 답 0 박힘 수정** — 모달 init 동그라미(①~⑤) 못읽어 0 + PATCH 0을 short_answer로 flip. 3중 차단 + prod 백필 63건. (메모리 갱신)
- #345 그룹 이동 모달 계층 트리 / #346 클라우드 목록 소속 폴더 배지(직속/하위) / #347 클라우드 검색 선택 폴더 범위로 한정
- #348 **뒤로가기 직전 페이지로** `useSmartBack(fallback)` — 21개 파일 적용(이력 있으면 back, 없으면 기존 경로 폴백). signup/solve 단계전환·이미 router.back 은 제외

## 주의 / 함정
- **배포 = main 머지만**(브랜치 push 는 preview, prod 아님). 사용자 지시: HML 은 로컬 검증 후 머지.
- 커밋 메시지는 here-string(`-F -` 또는 파일)으로 — PowerShell here-string 이 `:` `?` `"` `(` 에서 깨짐.
- 브랜치 작업 후 main 으로 돌아와 `git pull --ff-only`. PR 머지는 `gh pr merge --squash` 후 원격/로컬 브랜치 삭제.
- **API 비용 규율** — 유료 AI 일괄작업 임의 실행 금지(메모리 `feedback_api_cost_discipline`). HML import 는 의도적으로 분류 미실행(비용 0).
- 프리뷰 Supabase 미연동 — DB 의존 검증은 로컬 dev(실 Supabase) 또는 prod 머지 후.
- MCP Supabase project_id = `ppexawiiphghdrjnmvkx`.
